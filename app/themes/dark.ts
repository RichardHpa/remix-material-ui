import { createTheme } from "@mui/material";

export const darkTheme = createTheme({
  palette: {
    mode: "dark",
    background: {
      // default: "#111827",
      // paper: "#111827",
      default: "#c0392b",
      paper: "#c0392b",
    },
  },
});
